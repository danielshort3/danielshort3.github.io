{
  "name": "danielshort-website",
  "version": "1.0.0",
  "private": true,
  "description": "Simple tests for Daniel Short's website",
  "scripts": {
    "test": "node test.js",
    "build:icons": "python3 build/resize_logo.py",
    "build:css": "node build/build-css.js",
    "build:projects": "node build/generate-project-pages.js",
    "build": "npm run build:css && npm run build:projects && node build/copy-to-public.js"
  },
  "engines": {
    "node": ">=18"
  }
}
