diff --git a/demos/chatbot-demo.html b/demos/chatbot-demo.html
index e590f68..968dc9e 100644
--- a/demos/chatbot-demo.html
+++ b/demos/chatbot-demo.html
@@ -173,6 +173,8 @@
             display: flex;
             align-items: center;
             gap: 8px;
+            flex-wrap: wrap;
+            justify-content: flex-end;
         }
 
         .icon-btn {
@@ -302,7 +304,7 @@
 
         .stepper {
             display: grid;
-            grid-template-columns: repeat(3,1fr);
+            grid-template-columns: repeat(4,minmax(0,1fr));
             gap: 8px;
             align-items: center;
             font-size: .85rem;
@@ -856,6 +858,9 @@
                 </p>
             </div>
             <div class="header-actions">
+                <button id="warmup" class="btn-ghost" type="button">
+                    <i class="fa-solid fa-power-off"></i> Start server
+                </button>
                 <button id="clear-all" class="icon-btn" type="button" aria-label="Clear conversation">
                     <i class="fa-solid fa-broom"></i>
                 </button>
@@ -914,9 +919,9 @@
 
     <script src="js/demos/aws-client.js"></script>
     <script>
-        const DEFAULT_API_URL = 'https://ovodkr9oad.execute-api.us-east-2.amazonaws.com/prod';
-        const STORAGE_KEY = 'demo.endpoint.chatbot';
-        const { resolveEndpoint, getJson, retryRequest } = window.DemoAws;
+        const DEFAULT_API_URL = 'https://k8bys9gicf.execute-api.us-east-2.amazonaws.com/prod';
+        const STORAGE_KEY = 'demo.endpoint.chatbot.markdown';
+        const { resolveEndpoint, getJson, postJson, retryRequest } = window.DemoAws;
         const endpointConfig = { defaultUrl: DEFAULT_API_URL, storageKey: STORAGE_KEY };
         const API_URL = resolveEndpoint(endpointConfig).replace(/\/$/, '');
         const STATUS_URL = `${API_URL}/status`;
@@ -940,6 +945,7 @@
         const statusEl = document.getElementById('status');
         const answerEl = document.getElementById('answer');
         const askBtn = document.getElementById('ask');
+        const warmupBtn = document.getElementById('warmup');
         const stopBtn = document.getElementById('stop');
         const form = document.getElementById('chat-form');
         const clearBtn = document.getElementById('clear-all');
@@ -1073,6 +1079,11 @@
             askBtn.disabled = true;
             askBtn.setAttribute('aria-disabled', 'true');
             askBtn.title = DEMO_DISABLED_REASON;
+            if (warmupBtn) {
+                warmupBtn.disabled = true;
+                warmupBtn.setAttribute('aria-disabled', 'true');
+                warmupBtn.title = DEMO_DISABLED_REASON;
+            }
             promptEl.setAttribute('aria-disabled', 'true');
             promptEl.title = DEMO_DISABLED_REASON;
             if (stopBtn) {
@@ -1105,6 +1116,11 @@
             askBtn.disabled = false;
             askBtn.removeAttribute('aria-disabled');
             askBtn.removeAttribute('title');
+            if (warmupBtn) {
+                warmupBtn.disabled = false;
+                warmupBtn.removeAttribute('aria-disabled');
+                warmupBtn.removeAttribute('title');
+            }
             if (stopBtn) {
                 stopBtn.hidden = true;
                 stopBtn.disabled = true;
@@ -1130,8 +1146,9 @@
 
         /* ---------- Stepper ---------- */
         const steps = [
-            { id: 'submit', label: 'Submit prompt' },
-            { id: 'process', label: 'Processing on server' },
+            { id: 'submit', label: 'Request received' },
+            { id: 'boot', label: 'Start model' },
+            { id: 'process', label: 'Run model' },
             { id: 'complete', label: 'Complete' }
         ];
         function renderStepper() {
@@ -1246,9 +1263,9 @@
             parentBubble.appendChild(div);
         }
 
-        /* ---------- Server status (client-side timers only) ---------- */
-        const START_TIMEOUT_SEC = 600; // 10 minutes fixed
-        const GRACE_SEC = 300;         // 5 minute active window
+        /* ---------- Server status ---------- */
+        const START_TIMEOUT_SEC = 300; // conservative fallback; backend stage estimates refine this
+        const GRACE_SEC = 300;         // five-minute warm hold after output
         const STATUS_POLL_INTERVAL_MS = 60000; // Periodic status refresh cadence
 
         const STATE = {
@@ -1264,6 +1281,41 @@
         let graceTimer = null, graceDeadline = 0;
         let statusSyncPending = false;
         let statusPoller = null;
+        let serverReady = false;
+        let requestActive = false;
+
+        function updateAskButton() {
+            if (DEMO_DISABLED) return;
+            askBtn.disabled = requestActive || !serverReady;
+            askBtn.setAttribute('aria-disabled', String(askBtn.disabled));
+            askBtn.title = serverReady ? '' : 'Start the server before sending a query.';
+        }
+        function setWarmupButton(state) {
+            if (!warmupBtn || DEMO_DISABLED) return;
+            if (state === 'warm') {
+                warmupBtn.disabled = true;
+                warmupBtn.setAttribute('aria-disabled', 'true');
+                warmupBtn.innerHTML = '<i class="fa-solid fa-check"></i> Server ready';
+                warmupBtn.title = 'Server is warm and ready.';
+            } else if (state === 'starting') {
+                warmupBtn.disabled = true;
+                warmupBtn.setAttribute('aria-disabled', 'true');
+                warmupBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Starting';
+                warmupBtn.title = 'Server is starting.';
+            } else {
+                warmupBtn.disabled = false;
+                warmupBtn.setAttribute('aria-disabled', 'false');
+                warmupBtn.innerHTML = '<i class="fa-solid fa-power-off"></i> Start server';
+                warmupBtn.title = 'Start the server before asking questions.';
+            }
+        }
+        function setServerReady(value) {
+            serverReady = Boolean(value);
+            updateAskButton();
+            setWarmupButton(serverReady ? 'warm' : 'idle');
+        }
+        updateAskButton();
+        setWarmupButton('idle');
 
         const getStoredStartDeadline = () => restoreDeadline(STORAGE_KEYS.startDeadline);
         const getStoredGraceDeadline = () => restoreDeadline(STORAGE_KEYS.graceDeadline);
@@ -1346,6 +1398,7 @@
             const { remainingSec = null, useStoredDeadline = false, forceReset = false } = options;
             recordLog('status:online', { source, remainingSec, forceReset });
             stopStartingTimer();
+            setServerReady(true);
             let deadlineMs = null;
             if (typeof remainingSec === 'number' && remainingSec > 0) {
                 deadlineMs = Date.now() + remainingSec * 1000;
@@ -1380,16 +1433,23 @@
                     return ts ? Math.max(0, Math.round((ts - Date.now()) / 1000)) : null;
                 })();
                 if (status.warmSeconds && status.warmSeconds > 0) {
+                    setServerReady(false);
+                    setWarmupButton('starting');
                     startStartingTimer({ durationSec: status.warmSeconds, force: true, reason: 'status-eta' });
                 } else if (storedRemaining && storedRemaining > 0) {
+                    setServerReady(false);
+                    setWarmupButton('starting');
                     startStartingTimer({ durationSec: storedRemaining, force: true, reason: 'storage-resume' });
                 } else if (typeof status.payload?.desired === 'number' && status.payload.desired > 0) {
+                    setServerReady(false);
+                    setWarmupButton('starting');
                     startStartingTimer({ durationSec: START_TIMEOUT_SEC, force: true, reason: 'pending-scale' });
                 } else {
                     currentState = STATE.OFFLINE_DETECTED;
                     setDot(currentState);
-                    svcText.textContent = 'Unavailable';
+                    svcText.textContent = 'Start server before asking.';
                     svcETA.textContent = '';
+                    setServerReady(false);
                 }
             }
             statusSyncPending = false;
@@ -1437,7 +1497,7 @@
             const shutMin = minutesRound(GRACE_SEC);
             const warmLabel = describeMinutesLabel(warmMin);
             const shutLabel = describeMinutesLabel(shutMin);
-            body.textContent = `To control hosting costs, the demo server starts from off on your first message (cold start, up to about ${warmLabel}), and automatically shuts down after about ${shutLabel} of inactivity. Your very first response may take longer than usual.`;
+            body.textContent = `To control hosting costs, the demo server starts from off on demand (cold start, up to about ${warmLabel}). Use Start server to warm it before asking questions. The status bar updates as AWS queues, boots, and runs the model, then the server stays warm for about ${shutLabel} after the last output.`;
 
             function close(){
                 try { if (never && never.checked) localStorage.setItem(LS_KEY, '1'); } catch {}
@@ -1469,6 +1529,8 @@
             const minutes = Math.max(1, Math.round(durationSec / 60));
             currentState = STATE.STARTING;
             setDot(currentState);
+            setServerReady(false);
+            setWarmupButton('starting');
             const label = describeMinutesLabel(minutes);
             svcText.textContent = `Starting... (up to ~${label})`;
             startingDeadline = Date.now() + Math.max(1, Math.round(durationSec)) * 1000;
@@ -1502,6 +1564,7 @@
             stopGraceCycle({ clearStorage: false });
             currentState = STATE.ONLINE_GRACE;
             setDot(currentState);
+            setServerReady(true);
             const now = Date.now();
             if (typeof deadlineMs === 'number' && deadlineMs > now) {
                 graceDeadline = deadlineMs;
@@ -1531,8 +1594,9 @@
                     if (!status.online) {
                         currentState = STATE.SHUTDOWN;
                         setDot(currentState);
-                        svcText.textContent = 'Unavailable (send a message to start it again)';
+                        svcText.textContent = 'Start server before asking.';
                         svcETA.textContent = '';
+                        setServerReady(false);
                     } else if (typeof status.payload?.shutdownSeconds === 'number' && status.payload.shutdownSeconds > 0) {
                         markServerOnline('grace-refresh', {
                             remainingSec: status.payload.shutdownSeconds,
@@ -1541,8 +1605,9 @@
                     } else if (typeof status.payload?.current === 'number' && status.payload.current <= 0) {
                         currentState = STATE.SHUTDOWN;
                         setDot(currentState);
-                        svcText.textContent = 'Unavailable (send a message to start it again)';
+                        svcText.textContent = 'Start server before asking.';
                         svcETA.textContent = '';
+                        setServerReady(false);
                     } else {
                         markServerOnline('grace-refresh', { useStoredDeadline: true });
                     }
@@ -1558,6 +1623,74 @@
             recordLog('ui:first-request');
             if (currentState === STATE.OFFLINE_DETECTED) startStartingTimer();
         }
+        async function warmupServer() {
+            if (DEMO_DISABLED || !warmupBtn || warmupBtn.disabled) return;
+            recordLog('warmup:init');
+            setServerReady(false);
+            setWarmupButton('starting');
+            setStep('submit', 'in-progress');
+            setStep('boot', 'in-progress');
+            setStep('process', 'not-started');
+            setStep('complete', 'not-started');
+            startStartingTimer({ durationSec: START_TIMEOUT_SEC, force: true, reason: 'manual-warmup' });
+
+            try {
+                const submit = await postJson(`${API_URL}/warmup`, {});
+                recordLog('warmup:submit', { status: submit.status, jobId: submit.jobId, warm: submit.warm });
+                if (submit.status === 'READY' || submit.warm || !submit.jobId) {
+                    setStep('submit', 'done');
+                    setStep('boot', 'done');
+                    setStep('process', 'done');
+                    setStep('complete', 'done');
+                    markServerOnline('warmup-ready', {
+                        remainingSec: submit.warm_hold_seconds || GRACE_SEC,
+                        forceReset: true
+                    });
+                    return;
+                }
+
+                async function pollWarmup() {
+                    const query = submit.jobId
+                        ? `jobId=${encodeURIComponent(submit.jobId)}`
+                        : `outputUri=${encodeURIComponent(submit.outputUri)}`;
+                    const res = await getJson(`${API_URL}/result?${query}`, { cache: 'no-store' });
+                    const stageName = String(res?.stage?.name || '').toLowerCase();
+                    recordLog('warmup:poll', { status: res.status, stage: stageName });
+                    if (stageName === 'processing') {
+                        setStep('submit', 'done');
+                        setStep('boot', 'done');
+                        setStep('process', 'in-progress');
+                    }
+                    if (res.status === 'READY') {
+                        setStep('submit', 'done');
+                        setStep('boot', 'done');
+                        setStep('process', 'done');
+                        setStep('complete', 'done');
+                        markServerOnline('warmup-complete', {
+                            remainingSec: submit.warm_hold_seconds || GRACE_SEC,
+                            forceReset: true
+                        });
+                        return;
+                    }
+                    if (res.status === 'FAILED') {
+                        throw new Error(res.error || 'Warm-up failed');
+                    }
+                    await delay(2000);
+                    return pollWarmup();
+                }
+                await pollWarmup();
+            } catch (err) {
+                recordLog('warmup:error', { error: describeError(err) }, 'error');
+                setServerReady(false);
+                setWarmupButton('idle');
+                stopStartingTimer();
+                svcText.textContent = `Warm-up failed: ${describeError(err)}`;
+                svcETA.textContent = '';
+                setStep('boot', 'not-started');
+                setStep('process', 'not-started');
+                setStep('complete', 'not-started');
+            }
+        }
         /* ---------- Networking ---------- */
         let abort = null; let cancelled = false;
         async function ask(prompt, onStatus = () => { }, retryCount = 0) {
@@ -1590,7 +1723,7 @@
                 throw withRequestId(err, requestId);
             }
 
-            if (submitResponse.status === 202 || submit?.starting) {
+            if (submit?.starting) {
                 if (retryCount > 2) {
                     throw withRequestId(new Error('Server is still starting. Please try again shortly.'), requestId);
                 }
@@ -1621,7 +1754,10 @@
                     recordLog('poll:cancelled', { requestId, pollAttempt });
                     throw withRequestId(new Error('stopped'), requestId);
                 }
-                const url = `${API_URL}/result?outputUri=${encodeURIComponent(outputUri)}`;
+                const query = submit.jobId
+                    ? `jobId=${encodeURIComponent(submit.jobId)}`
+                    : `outputUri=${encodeURIComponent(outputUri)}`;
+                const url = `${API_URL}/result?${query}`;
                 let pollResponse;
                 try {
                     pollResponse = await fetch(url, { signal: abort.signal });
@@ -1686,9 +1822,18 @@
             if (e) e.preventDefault();
             if (DEMO_DISABLED) return;
             const prompt = promptEl.value.trim(); if (!prompt) return;
+            if (!serverReady) {
+                svcText.textContent = 'Start the server before sending a query.';
+                svcETA.textContent = '';
+                warmupBtn?.focus();
+                recordLog('ui:submit-blocked-cold');
+                return;
+            }
             recordLog('ui:submit', { promptLength: prompt.length });
             onFirstUserRequest();
-            setStep('submit', 'in-progress'); askBtn.disabled = true; stopBtn.hidden = false; form.setAttribute('aria-busy', 'true');
+            requestActive = true;
+            updateAskButton();
+            setStep('submit', 'in-progress'); stopBtn.hidden = false; form.setAttribute('aria-busy', 'true');
             const { contentEl, bubble } = createExchange(prompt);
             promptEl.value = ''; autoResize();
 
@@ -1705,15 +1850,19 @@
                 const urls = data.source_urls || data.sources || data.urls; renderSources(bubble, urls);
                 recordLog('ui:rendered', { requestId, sourceCount: Array.isArray(urls) ? urls.length : 0 });
             } catch (err) {
+                setStep('boot', 'not-started');
                 setStep('process', 'not-started'); setStep('complete', 'not-started');
                 contentEl.innerHTML = `<div><strong>Error:</strong> ${String(err.message || err)}</div>`;
                 recordLog('ui:error', { requestId: err?.requestId, error: describeError(err) }, 'error');
             } finally {
-                askBtn.disabled = false; stopBtn.hidden = true; form.setAttribute('aria-busy', 'false');
+                requestActive = false;
+                updateAskButton();
+                stopBtn.hidden = true; form.setAttribute('aria-busy', 'false');
                 saveHistory(); notifyResize();
             }
         }
         askBtn.addEventListener('click', handleSubmit);
+        warmupBtn?.addEventListener('click', warmupServer);
         form.addEventListener('submit', handleSubmit);
 
         /* ---------- Quick Prompts marquee (JS-driven) ---------- */
diff --git a/test.js b/test.js
index 59c18cf..488b601 100644
--- a/test.js
+++ b/test.js
@@ -1394,9 +1394,16 @@ try {
 
   section('Chatbot demo startup timer', () => {
     const chatbotHtml = fs.readFileSync('demos/chatbot-demo.html', 'utf8');
+    const vercel = fs.readFileSync('vercel.json', 'utf8');
+    assert(chatbotHtml.includes("const DEFAULT_API_URL = 'https://k8bys9gicf.execute-api.us-east-2.amazonaws.com/prod';"), 'chatbot-demo missing new API URL');
+    assert(!chatbotHtml.includes('ovodkr9oad'), 'chatbot-demo still references old API');
+    assert(chatbotHtml.includes("postJson(`${API_URL}/warmup`"), 'chatbot-demo missing warm-up API call');
+    assert(chatbotHtml.includes('let serverReady = false;'), 'chatbot-demo should gate sending on warm server state');
+    assert(vercel.includes('k8bys9gicf.execute-api.us-east-2.amazonaws.com'), 'vercel.json missing new chatbot API host');
+    assert(!vercel.includes('ovodkr9oad.execute-api.us-east-2.amazonaws.com'), 'vercel.json still allows old chatbot API host');
     const startConst = chatbotHtml.match(/const START_TIMEOUT_SEC = (\d+);/);
     const warmSec = startConst ? parseInt(startConst[1], 10) : 0;
-    assert(warmSec >= 600, 'chatbot-demo start timeout < 10 minutes');
+    assert(warmSec >= 270, 'chatbot-demo start timeout < measured cold-start estimate');
 
     const startSection = chatbotHtml.match(/\/\/ Starting timer helpers[\s\S]*?\/\/ End starting timer helpers/);
     assert(startSection, 'chatbot-demo starting timer section missing');
@@ -1416,6 +1423,8 @@ try {
       svcText: { textContent: '' },
       svcETA: { textContent: '' },
       setDot: () => {},
+      setServerReady: () => {},
+      setWarmupButton: () => {},
       startGraceCycle: () => {},
       fmtClock: () => '',
       setInterval: () => 1,
@@ -1423,10 +1432,10 @@ try {
     };
     vm.runInNewContext(startSection[0], warmEnv);
     warmEnv.startStartingTimer();
-    warmEnv.Date.now = () => 599 * 1000;
+    warmEnv.Date.now = () => (warmSec - 1) * 1000;
     assert(warmEnv.currentStartingRemaining() > 0, 'starting countdown ended too early');
-    warmEnv.Date.now = () => 601 * 1000;
-    assert(warmEnv.currentStartingRemaining() === 0, 'starting countdown did not finish after ten minutes');
+    warmEnv.Date.now = () => (warmSec + 1) * 1000;
+    assert(warmEnv.currentStartingRemaining() === 0, 'starting countdown did not finish after configured startup budget');
   });
 
   section('404 rewrites and portfolio page sections', () => {
diff --git a/vercel.json b/vercel.json
index 2eacfec..99dba2f 100644
--- a/vercel.json
+++ b/vercel.json
@@ -340,7 +340,7 @@
         { "key": "Strict-Transport-Security", "value": "max-age=31536000; includeSubDomains; preload" },
         {
           "key": "Content-Security-Policy",
-          "value": "default-src 'self'; img-src 'self' data: blob: https:; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com; font-src 'self' https://fonts.gstatic.com https://cdnjs.cloudflare.com; media-src 'self' blob:; script-src 'self' blob: 'unsafe-inline' 'unsafe-eval' 'wasm-unsafe-eval' https://www.googletagmanager.com https://www.google-analytics.com https://cdn.jsdelivr.net https://staticimgly.com https://cdnjs.cloudflare.com https://public.tableau.com; script-src-elem 'self' blob: 'unsafe-inline' 'unsafe-eval' 'wasm-unsafe-eval' https://www.googletagmanager.com https://www.google-analytics.com https://cdn.jsdelivr.net https://staticimgly.com https://cdnjs.cloudflare.com https://public.tableau.com; worker-src 'self' blob: https://cdn.jsdelivr.net https://staticimgly.com; child-src 'self' blob: https://cdn.jsdelivr.net https://staticimgly.com; connect-src 'self' blob: https://www.google-analytics.com https://region1.google-analytics.com https://www.googletagmanager.com https://cdn.jsdelivr.net https://staticimgly.com https://job-tracker-auth-886623862678.auth.us-east-2.amazoncognito.com https://*.s3.amazonaws.com https://*.s3.us-east-2.amazonaws.com https://ovodkr9oad.execute-api.us-east-2.amazonaws.com https://fhp2is6v8h.execute-api.us-east-2.amazonaws.com https://4kvebym8b3.execute-api.us-east-2.amazonaws.com https://public.tableau.com https://zcosyfxhs3sntpwzo3qayki2he0kdxkw.lambda-url.us-east-2.on.aws https://7aa3jt3tzkinheprc5ds52bihq0itutx.lambda-url.us-east-2.on.aws https://gw676aqgd4lpcoxngfvnfki5ce0urcfg.lambda-url.us-east-2.on.aws https://wklw5cb75voj4idwbnigdt6p7a0zrxwv.lambda-url.us-east-2.on.aws https://w7jbsr55oggohong42ii6wo3jq0bvkvq.lambda-url.us-east-2.on.aws https://lv4inwnj6yyo3kfdajpk2v5eda0hrtao.lambda-url.us-east-2.on.aws https://2d6lrg4ozy564xymwi2epjbqty0nvhje.lambda-url.us-east-2.on.aws https://e7axe2ymmx2kadovfvvji23wkq0xmybn.lambda-url.us-east-2.on.aws https://oafstar74okqvfqaxayeici3ry0yikkx.lambda-url.us-east-2.on.aws https://jnvd3mdbyb5f44yh4afzsvqlwy0mdtzy.lambda-url.us-east-2.on.aws https://coxbbervgzwhm5tu53dutxwfca0vxdkg.lambda-url.us-east-2.on.aws; frame-src 'self' https://public.tableau.com https://docs.google.com;"
+          "value": "default-src 'self'; img-src 'self' data: blob: https:; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com; font-src 'self' https://fonts.gstatic.com https://cdnjs.cloudflare.com; media-src 'self' blob:; script-src 'self' blob: 'unsafe-inline' 'unsafe-eval' 'wasm-unsafe-eval' https://www.googletagmanager.com https://www.google-analytics.com https://cdn.jsdelivr.net https://staticimgly.com https://cdnjs.cloudflare.com https://public.tableau.com; script-src-elem 'self' blob: 'unsafe-inline' 'unsafe-eval' 'wasm-unsafe-eval' https://www.googletagmanager.com https://www.google-analytics.com https://cdn.jsdelivr.net https://staticimgly.com https://cdnjs.cloudflare.com https://public.tableau.com; worker-src 'self' blob: https://cdn.jsdelivr.net https://staticimgly.com; child-src 'self' blob: https://cdn.jsdelivr.net https://staticimgly.com; connect-src 'self' blob: https://www.google-analytics.com https://region1.google-analytics.com https://www.googletagmanager.com https://cdn.jsdelivr.net https://staticimgly.com https://job-tracker-auth-886623862678.auth.us-east-2.amazoncognito.com https://*.s3.amazonaws.com https://*.s3.us-east-2.amazonaws.com https://k8bys9gicf.execute-api.us-east-2.amazonaws.com https://fhp2is6v8h.execute-api.us-east-2.amazonaws.com https://4kvebym8b3.execute-api.us-east-2.amazonaws.com https://public.tableau.com https://zcosyfxhs3sntpwzo3qayki2he0kdxkw.lambda-url.us-east-2.on.aws https://7aa3jt3tzkinheprc5ds52bihq0itutx.lambda-url.us-east-2.on.aws https://gw676aqgd4lpcoxngfvnfki5ce0urcfg.lambda-url.us-east-2.on.aws https://wklw5cb75voj4idwbnigdt6p7a0zrxwv.lambda-url.us-east-2.on.aws https://w7jbsr55oggohong42ii6wo3jq0bvkvq.lambda-url.us-east-2.on.aws https://lv4inwnj6yyo3kfdajpk2v5eda0hrtao.lambda-url.us-east-2.on.aws https://2d6lrg4ozy564xymwi2epjbqty0nvhje.lambda-url.us-east-2.on.aws https://e7axe2ymmx2kadovfvvji23wkq0xmybn.lambda-url.us-east-2.on.aws https://oafstar74okqvfqaxayeici3ry0yikkx.lambda-url.us-east-2.on.aws https://jnvd3mdbyb5f44yh4afzsvqlwy0mdtzy.lambda-url.us-east-2.on.aws https://coxbbervgzwhm5tu53dutxwfca0vxdkg.lambda-url.us-east-2.on.aws; frame-src 'self' https://public.tableau.com https://docs.google.com;"
         }
       ]
     },
